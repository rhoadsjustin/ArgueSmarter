import React from 'react';
import { Header, Item, Container, Content, Input, Thumbnail, Text, Button, Icon, Left, Body } from 'native-base';
import { View } from 'react-native';
import styles from './styles';

export default (props) => (
  
);
